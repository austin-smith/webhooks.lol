---
title: Endpoints
description: Receive URLs, the paths and methods they accept, naming, and switching between them.
icon: Webhook
---

An endpoint is a private receive URL that captures the requests sent to it.
Opening the app creates one for you, and you can create more at any time.

## The receive URL

Each endpoint has a receive URL shaped like:

```text
https://webhooks.lol/api/hook/<endpoint-id>
```

The endpoint ID is a random UUID. Anyone with the receive URL can send requests
to it. Captured traffic for anonymous endpoints is readable by endpoint ID;
captured traffic for account-owned endpoints requires the owning browser
session.

Use the copy button next to the URL in the inspector to grab it.

## Paths and query strings

You can send to the receive URL directly or to any path beneath it. The path is
captured and shown with each request, so a sender can use whatever path it
normally would:

```bash
curl https://webhooks.lol/api/hook/<endpoint-id>/any/path?key=value
```

Query strings are captured and parsed too, including repeated keys.

## Methods

Endpoints accept `GET`, `POST`, `PUT`, `PATCH`, `DELETE`, and `HEAD`. The method
is recorded with every captured request.

Receive URLs return permissive CORS headers, so you can send requests to them
from browser JavaScript. Browser preflight (`OPTIONS`) requests are answered
automatically and are **not** saved, so they do not clutter the list.

## Naming an endpoint

New endpoints are identified by a shortened form of their ID. Give an endpoint a
name to recognize it later: open the endpoint switcher, click the pencil on the
selected endpoint, type a label (up to 32 characters), and save. The name is
only a local label and does not change the receive URL.

## Switching between endpoints

The switcher at the top left lists endpoints for the current session, most
recent first. Anonymous sessions show endpoints remembered by this browser.
Signed-in sessions show endpoints attached to the account, plus same-browser
anonymous endpoints that are still available.

The switcher supports:

- Searching by name or ID.
- Opening a previously used endpoint.
- Creating an endpoint with a new receive URL.
- Claiming an anonymous endpoint to your account when one is available.

<Callout type="warn">
  The service keeps up to 50 endpoints per signed-in account or anonymous
  browser session. Creating another endpoint past that limit succeeds and
  removes the least recently active endpoint.
</Callout>
